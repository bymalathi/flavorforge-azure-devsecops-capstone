apiVersion: apps/v1
kind: Deployment

metadata:
  name: frontend
  namespace: flavorforge

  labels:
    app: frontend

spec:
  replicas: 2

  selector:
    matchLabels:
      app: frontend

  template:
    metadata:
      labels:
        app: frontend

    spec:
      automountServiceAccountToken: false
      
      containers:
        - name: frontend

          image: flavorforgeacr2026ms.azurecr.io/flavorforge-frontend:1.3

          imagePullPolicy: Always

          ports:
            - containerPort: 80

          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
              ephemeral-storage: "256Mi"

            limits:
              cpu: "500m"
              memory: "512Mi"
              ephemeral-storage: "1000Mi"
